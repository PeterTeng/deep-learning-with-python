import numpy as np

# A = np.array([[1, 2], [3, 4]])
# B = np.array([[5, 6], [7, 8]])

# multiple = np.dot(A, B)

# print(multiple)

X = np.array([1, 2])
print(X)

W = np.array([[1, 3, 5], [2, 4, 6]])
print(W)

Y = np.dot(X, W)

print(Y)
